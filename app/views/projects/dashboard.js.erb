$("#m").html("<%= escape_javascript(render(partial: "card", locals: {id: "To do", project: @project, count: @project.get_tasks_to_do.search(params[:search], current_user).count, tickets: @project.get_tasks_to_do.search(params[:search], current_user)})) %>");
$("#m1").html("<%= escape_javascript(render(partial: "card", locals: {id: "In progress", project: @project, count: @project.get_tasks_in_progress.search(params[:search], current_user).count, tickets: @project.get_tasks_in_progress.search(params[:search], current_user)})) %>");
$("#m2").html("<%= escape_javascript(render(partial: "card", locals: {id: "Dev complete", project: @project, count: @project.get_tasks_dev_complete.search(params[:search], current_user).count, tickets: @project.get_tasks_dev_complete.search(params[:search], current_user)})) %>");
$("#m3").html("<%= escape_javascript(render(partial: "card", locals: {id: "Done", project: @project, count: @project.get_tasks_done.search(params[:search], current_user).count, tickets: @project.get_tasks_done.search(params[:search], current_user)})) %>");
